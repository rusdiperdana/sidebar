<nav class="navbar navbar-expand-sm navbar-dark mb-3" style="background-color: #821B1C; background-image: url('{{ asset('assets/img/banner_QR.jpg') }}');background-repeat: no-repeat; height: 90px">    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="navbar-nav ml-auto" id="mobile-nav">
            <ul class="nav nav-pills">
                {% if is_granted("ROLE_TATA_USAHA") %}
                <li class="nav-item dropdown">
                    <a class="nav-link" href="#">Kegiatan</a>
                </li>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item dropdown">
                    <a class="nav-link" href="{{ path('register_user') }}">User</a>
                </li>
                {% endif %}
                {% if is_granted("ROLE_USER")%}
                <li class="nav-item dropdown">
                    <a class="nav-link" href="#">Lihat index</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>